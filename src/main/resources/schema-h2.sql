CREATE TABLE Employee ( id BIGINT GENERATED BY DEFAULT AS IDENTITY ( START WITH 1 ) PRIMARY KEY, FIRSTNAME VARCHAR(100), LASTNAME VARCHAR(100), ROLE VARCHAR(20))
CREATE TABLE Manager ( manager_id BIGINT GENERATED BY DEFAULT AS IDENTITY ( START WITH 1) PRIMARY KEY, NAME VARCHAR(100))
